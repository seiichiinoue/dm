#pragma once
#include <cassert>
#include <cmath>
#include <random>
#include <fstream>
using namespace std;

namespace dm {
    class DM {
    public:
        double *lambda;
        double **alpha;
    };
};
